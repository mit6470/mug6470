<% if current_user %>
  <h2>Projects</h2>
  <%= link_to 'New Project', new_project_path, :class => :button %> 
  <ul>
    <% current_user.projects.each do |project| %>
      <li>  
        <h3><%= link_to "#{project.name}", edit_project_path(project) %></h3>
        <%= link_to 'Destroy', project, confirm: 'Are you sure?', method: :delete %>
      </li>
    <% end %>
  </ul>
<% else %>
  <h3>Tips</h3>
  <ul>
    <li>
      <span class="ui-icon ui-icon-lightbulb"></span>
      <p>
        After 
        <%= link_to 'signing up', new_user_path %>
        for an account, you can start your own projects.
      </p>
    </li>
    <li>
      <span class="ui-icon ui-icon-lightbulb"></span>
      <p>
        Trials in a project are saved so that you can come back later to compare
        your results.
      </p>
    </li>
    <li>
      <span class="ui-icon ui-icon-lightbulb"></span>
      <p>
        Once you have an account, you can upload your own data set for analysis.
      </p>
    </li>
  </ul>
<% end %>