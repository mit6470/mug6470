<% if @datum %>
  <table>
    <thead>
      <tr>
        <th><%= @datum.features[-1][:name] %></th>
      <% @datum.features[1...-1].each do |f| %>
        <th><%= f[:name] %></th>
      <% end %>
      </tr>
    </thead>
    <tbody>
      <% @examples && @examples.select { |e| !e.nil? }.each do |e| %>
        <tr>
          <td><%= e[-1] %></td>
          <% e[1...-1].each do |v| %>
            <td><%= v %></td>
          <% end %>
        </tr>
      <% end %>
    </tbody>
  </table>
<% end %>
